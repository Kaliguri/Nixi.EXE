# --- Голосовой стек (ставится отдельно, тяжелее) ---
# Запись/воспроизведение звука
sounddevice>=0.4.6
soundfile>=0.12
numpy>=1.24

# STT (распознавание речи) — на GPU через CUDA
faster-whisper>=1.0

# Wake word: «дежурное» прослушивание фразы активации (Vosk-споттер)
vosk>=0.3.45

# TTS (синтез речи):
#   edge-tts — нейросетевой русский голос Microsoft (бесплатно, без ключа, нужен интернет) — по умолчанию
#   pyttsx3  — офлайн через системный SAPI5 (фолбэк; для русского нужен русский голос в Windows)
edge-tts>=6.1
pyttsx3>=2.90

# Wake word (опционально; по умолчанию используется push-to-talk)
# openwakeword>=0.6
