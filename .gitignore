# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets
.env

# Личная калибровка wake word (под конкретный голос)
wake_phrases.txt

# Models / caches (скачиваются при первом запуске)
models/
# Референс-клипы голоса для XTTS (генерит setup_xtts.py / кладёт пользователь)
voices/
*.onnx
*.bin
.cache/
audio_cache/

# Трей: ярлык и иконка генерятся install-tray.ps1 под конкретную машину + логи сервера
/*.lnk
assets/tray/
logs/

# Сборка exe (PyInstaller)
.venv-build/
/build/
*.exe

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (на всякий случай; вложенный apps/web/.gitignore тоже покрывает)
node_modules/
dist/

# Локальные настройки Claude Code (личные, не для репозитория)
.claude/settings.local.json
